<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="AfflSQL">
	<select id="AfflSQL.doSelectCount" resultType="int">
		/*
			SQL ID : AfflSQL.doSelectCount
			연구개발산업서비스 가입여부 확인
		*/
		select count(*)
		from ta_affl_more
		where rndregisterapprovalnumber = #{rndregisterapprovalnumber}
	</select>	
</mapper>